<Page
    x:Class="Selfwin.Items.ReadItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Selfwin.Items"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer  Margin="15 13">
            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding Parameter.Title}"
                           Style="{StaticResource SubtitleTextBlockStyle}" />

                <TextBlock Text="{Binding Parameter.SourceTitle}"
                           HorizontalAlignment="Right"
                           Grid.Row="1" />

                <TextBlock Text="{Binding Parameter.Content}"
                           TextWrapping="WrapWholeWords"
                           Grid.Row="2" />
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Button Content="Starred" Style="{StaticResource ActionButtonStyle}" />
            <Button Content="Read" Style="{StaticResource ActionButtonStyle}" />
            <Button Content="Close" Style="{StaticResource ActionButtonStyle}" />
        </StackPanel>

    </Grid>
</Page>