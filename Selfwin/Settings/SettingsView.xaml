<Page
    x:Class="Selfwin.Settings.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Selfwin.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          Padding="15 0">
        <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0 15"/>
        
        <TextBlock Text="Server URL" Style="{StaticResource SubtitleTextBlockStyle}" />
        <TextBox x:Name="Url" />

        <TextBlock Text="Server port" Style="{StaticResource SubtitleTextBlockStyle}" />
        <TextBox x:Name="Port" />
        
        <TextBlock Text="Username" Style="{StaticResource SubtitleTextBlockStyle}" />
        <TextBox x:Name="Username" />

        <TextBlock Text="Password" Style="{StaticResource SubtitleTextBlockStyle}" />
        <PasswordBox x:Name="Password" />

        <TextBlock x:Name="ErrorMessage" Style="{StaticResource BodyTextBlockStyle}"
                   Margin="0 20" />
    </StackPanel>
</Page>
